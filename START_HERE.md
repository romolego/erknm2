# 🚀 Быстрый старт - Веб-интерфейс ERKNM

## ✅ Всё готово к использованию!

### 🌐 Веб-интерфейс запущен и доступен по адресу:

# **http://localhost:5000**

---

## 📋 Что было сделано:

1. ✅ Установлены все зависимости Python
2. ✅ Установлен браузер Chromium для Playwright
3. ✅ Создан файл конфигурации `.env`
4. ✅ Создан веб-интерфейс для управления
5. ✅ Веб-сервер запущен

## 🎯 Первые шаги:

### 1. Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

### 2. На главной странице вы увидите:
- 📊 Статистику системы
- 🔘 Кнопки управления
- 📋 Вкладки с данными

### 3. Первое действие - инициализация БД:
- Нажмите кнопку **"Инициализировать БД"**
- Дождитесь сообщения об успешной инициализации

### 4. Запуск синхронизации:
- Нажмите кнопку **"Запустить синхронизацию"**
- Процесс может занять некоторое время
- Следите за прогрессом во вкладке **"Журнал"**

## ⚙️ Настройка базы данных

Если у вас еще не настроена PostgreSQL:

1. Убедитесь, что PostgreSQL установлен и запущен
2. Создайте базу данных:
   ```sql
   CREATE DATABASE erknm;
   ```
3. Отредактируйте файл `.env` в папке проекта:
   ```
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=erknm
   DB_USER=postgres
   DB_PASSWORD=ваш_пароль
   ```

## 📱 Возможности веб-интерфейса:

### Главная страница:
- **Статистика** - общая информация о системе
- **Кнопки управления** - инициализация, синхронизация, обновление

### Вкладка "Запуски":
- Список всех запусков синхронизации
- Статус каждого запуска
- Количество обработанных файлов и записей

### Вкладка "Журнал":
- Детальный журнал всех операций
- Фильтрация по уровню (INFO, WARNING, ERROR)
- Автоматическое обновление

### Вкладка "Загрузка файла":
- Загрузка ZIP или XML файлов вручную
- Drag & Drop поддержка
- Автоматическая обработка загруженных файлов

## 🔄 Автообновление

Веб-интерфейс автоматически обновляет данные каждые 5 секунд, так что вы всегда видите актуальную информацию.

## 🛑 Остановка сервера

Для остановки веб-сервера нажмите `Ctrl+C` в терминале, где он запущен.

## 📞 Если что-то не работает:

1. **Сервер не запускается** - проверьте, что порт 5000 свободен
2. **Ошибка подключения к БД** - проверьте параметры в `.env`
3. **Ошибка при синхронизации** - проверьте доступность сайта proverki.gov.ru

---

## 🎉 Готово! Откройте http://localhost:5000 и начните работу!








